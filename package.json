{
  "name": "nuxt-intercom",
  "version": "1.0.3",
  "description": "NuxtJS module for Intercom",
  "repository": "github:dogchef-be/nuxt-intercom",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "main": "dist/module.js",
  "types": "dist/module.d.ts",
  "scripts": {
    "clean": "npm prune && rm -rf dist",
    "transpile": "tsc",
    "build": "npm run clean && npm run transpile"
  },
  "dependencies": {
    "@types/intercom-web": "^2.8.10"
  },
  "devDependencies": {
    "@nuxt/types": "^2.14.3",
    "@nuxt/typescript-build": "^0.6.7",
    "nuxt": "^2.14.3",
    "typescript": "^3.9.7"
  },
  "publishConfig": {
    "access": "public"
  }
}
